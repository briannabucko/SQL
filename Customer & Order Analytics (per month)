#In this SQL, I'm querying a database with multiple tables in it to quantify statistics about customer and order data. 

# Part 1: How many orders were placed in January?
SELECT COUNT(orderID)
FROM BIT_DB.JanSales
WHERE length(orderID) = 6
AND orderID <> 'Order ID';

# Part 2: How many of those orders were for an iPhone?
SELECT COUNT(orderID)
FROM BIT_DB.JanSales
WHERE length(orderID) = 6
AND orderID <> 'Order ID'
AND Product='iPhone';

# Part 3: Select the customer account numbers for all the orders that were placed in February.
SELECT DISTINCT acctnum
FROM BIT_DB.customers AS cust
INNER JOIN BIT_DB.Febsales AS feb
ON feb.orderID=cust.order_ID
WHERE length(orderID) = 6
AND orderID <> 'Order ID';

# Part 4: Which product was the cheapest one sold in January, and what was the price?
SELECT Product,
price 
FROM BIT_DB.JanSales
ORDER BY price ASC
LIMIT 1;

# Part 5: What is the total revenue for each product sold in January? 
SELECT sum(quantity)*price AS revenue,
product
FROM BIT_DB.JanSales
GROUP BY product;

# Part 6: Which products were sold in February at 548 Lincoln St, Seattle, WA 98101. How many of each were sold, and what was the total revenue?
SELECT sum(quantity)*price as revenue,
Product,
sum(quantity)
FROM BIT_DB.FebSales
WHERE location = '548 Lincoln St, Seattle, WA 98101';

# Part 7: How many customers ordered more than 2 products at a time in February, and what was the average amount spent for those customers?
SELECT count(distinct cust.acctnum),
avg(quantity*price)
FROM BIT_DB.FebSales Feb
LEFT JOIN BIT_DB.customers cust
ON FEB.orderid=cust.order_id
WHERE Feb.Quantity>2
AND length(orderid) = 6
AND orderid <> 'Order ID';

# Part 7: List all the products sold in Los Angeles in February, and include how many of each were sold.
SELECT * FROM BIT_DB.FebSales;
SELECT Product,
SUM(Quantity)
FROM BIT_DB.FebSales
WHERE location like '%Los Angeles%'
GROUP BY Product;

# Part 8: Which locations in New York received at least 3 orders in January, and how many orders did they each receive? (Hint: use HAVING).
SELECT distinct location, count(orderID)
FROM BIT_DB.JanSales
WHERE location LIKE '%NY%'
AND length(orderid) = 6 
AND orderid <> 'Order ID'
GROUP BY location
HAVING count(orderID)>2;

# Part 9: How many of each type of headphone were sold in February?
SELECT * FROM BIT_DB.FebSales LIMIT 5;
SELECT sum(Quantity),
product
FROM BIT_DB.FebSales
WHERE product LIKE '%headphone%'
GROUP BY product;

# Part 10: What was the average amount spent per account in February? 
SELECT COUNT(acctnum) AS accts,
AVG(price * quantity) AS avg_price
FROM BIT_DB.FebSales AS Feb
INNER JOIN BIT_DB.customers AS cust
ON Feb.orderID=cust.order_id
AND length(orderid) = 6 
AND orderid <> 'Order ID';

# Part 11: What was the average quantity of products purchased per account in February? 
SELECT sum(quantity)/count(cust.acctnum) AS avg_products_purchased
FROM BIT_DB.FebSales Feb
LEFT JOIN BIT_DB.customers cust
ON FEB.orderid=cust.order_id
WHERE length(orderid) = 6 
AND orderid <> 'Order ID';

# Part 12: Which product brought in the most revenue in January and how much revenue did it bring in total?
SELECT  Sum(price*quantity) AS total_rev,
product
FROM BIT_DB.JanSales
GROUP BY product
ORDER BY total_rev desc
LIMIT 1;

